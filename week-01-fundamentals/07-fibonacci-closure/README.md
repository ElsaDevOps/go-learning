# Fibonacci closure

a fibonacci function that returns a function (a closure) that returns successive fibonacci numbers (0, 1, 1, 2, 3, 5, ...).

## What I learned

- How to use function closures to reference variables from outside the function body


# Running

go run main.go